import { LocaleTranslatorPipe } from './locale-translator.pipe';

describe('LocaleTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new LocaleTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
